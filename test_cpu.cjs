const { Client } = require('ssh2'); const fs = require('fs'); const conn = new Client(); conn.on('ready', () => { conn.exec('ps -eo pid,cmd,%cpu --sort=-%cpu | head -n 15', (err, stream) => { if (err) throw err; let out = ''; stream.on('close', () => { fs.writeFileSync('cpu_result.txt', out, 'utf8'); conn.end(); }).on('data', (d) => { out += d; }); }); }).connect({ host: '84.247.184.208', port: 22, username: 'root', privateKey: fs.readFileSync('C:/Users/MSI/Desktop/vps_ssh_key', 'utf8') });
